# Everything else this file used to list — target/, HELP.md, *.db* and the IDE
# directories — is already matched at any depth by the root .gitignore. Only the
# two entries below are not, so only they live here.

# The wrapper downloads this; the repo ships maven-wrapper.properties alone.
.mvn/wrapper/maven-wrapper.jar

# Built frontend (docker build copies it in; never commit)
src/main/resources/static/
